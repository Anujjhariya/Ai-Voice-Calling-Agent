# ── Web Framework & Server ──
fastapi
uvicorn[standard]

# ── WebSocket ──
websockets

# ── Twilio (outbound calls + WhatsApp) ──
twilio

# ── Groq (STT Whisper + LLM Llama) ──
groq

# ── Google Calendar API ──
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# ── TTS audio conversion ──
imageio-ffmpeg

# ── Environment variables ──
python-dotenv

# ── HTTP requests (Exotel + Twilio recording download) ──
requests
httpx

# ── Database ──
# sqlite3 is built into Python — no install needed